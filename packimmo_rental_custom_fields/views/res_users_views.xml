<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_users_form_packimmo_partner" model="ir.ui.view">
        <field name="name">res.users.form.packimmo.partner</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='name']" position="before">
                <field name="partner_id" string="Contact lié" options="{'no_create': True}"/>
            </xpath>

        </field>
    </record>

</odoo>